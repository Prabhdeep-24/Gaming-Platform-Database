# Database connection URL (replace with your DB server info)
db.url=jdbc:mysql://localhost:3306/gamingPlatform?useSSL=false&serverTimezone=UTC

# Database username
db.user=root

# Database password
db.password=root

# Optional: JDBC driver class name
db.driver=com.mysql.cj.jdbc.Driver